

2019-08-08

1. csp genP10,为空 未抛出异常。   server端 抛出一个异常 : CA_Admin 替换了一个 jar

5. login 页面结束应用时，释放 key
       测试 kmc Admin  登录之后，关闭窗口是否 有 client 未释放

6. 测试 下载完证书 拔掉key ，最后释放 key会不会异常 

7. releaseKey

9. 中文问题，BO 申请证书  和  审计日志



2019-07-01
 2. 测试：
 
 2.1  验证  DN 是否支持 中英文 和 特殊符号 ：  （测试原则 ：本着 单符号可以的原则测试，但偶然测试出 《》 的问题，后已改正）
          中文              测试样例：  中文      
          英文符号       测试样例：    ~`   !@#$%^&*()    _+     []{}   ;':"   ,./ <>?    \|   .
	中文符号       测试样例：  ·~   ！@#￥%……&*（） ——+  -= 【】{}  ；‘：“  ，。、《》？   |   
    
  2.1.1 不支持的符号    +（下载证书时，提示DN拼写错误）    
  2.2.2 cn=user_,_ （英文） 提示管理员权限不正确 ===> DN修改为 cn="user_,_" 可以下载证书， 在 admin 查询显示 有   为  cn=user_\,_  ，在证书管理  cn=user_,_显示 正常
  2.2.3 特殊符号      "  在证书中不显示  =====>  暂时不管
 
2019-07-09
1. 验证 kmc 的 中文 和 特殊符号问题，====>通过 添加 CA 验证了 中文支持==> 逐一验证 唯一标识  CA名称，DN 项
           特殊符号要验一下：  全部都支持，有点不合理吧，但是 " + ,没有验，因为不能下载证书：key不够用，

2019-07-11
1.  关闭窗口时 提示已移除 key 
3.  Session   ： 只修改 大窗口 
4.  释放资源
10. 归档任务 0 为未完成， 2 为 暂停      1 显示 未完成   3 显示未知

会抛出 NeedPinException 的 方法有 ：   enumCert  genCSR   importCert  sign  importEncKeyPair  openContainer


2019-07-29  

1. 下载证书 的 combo 的 长度 由  csr 和 key 控制，
2. 下载管理员证书，csp产生 P10 的长度 固定是 1024, 
   修改：加 combo : 没有传 参数，因为后台不需要，模板或者说两码已经决定了密钥长度，出现提示框只是告诉他，知道自己下载的证书密钥长度，
             有问题，如果他选错了 长度，依然可下载证书，但是他就容易误解。

3. key显示的：HaiTai Cryptographic Service Provider 00001     登录能看到：XASJ Cryptographic Service Provider 31001   不管

5. 添加自定义OID：
   需求： 如果是扩展来源是CA端，并且 必填为是，必须要扩展值；如果来源是CLIENT端，不能写扩展值， 
   测试： 如果扩展设置为 CA ，在RADS下载证书时，默认会自动添加，不用自己添加。如果设置来源是CLIENT，需要在 RA端添加 OID、type、扩展值。
      证书效果： 扩展项名称是 oid,扩展值为 HEX类型。如果是 0730 扩展值显示 02020  2da(实际内容)， 16314519 显示 020400 f8f097(实际内容)
 
 补充：oid 的规则 ： 1/2 开头， 之后的数值 比较小.

2019-07-30 
1. 修改模板项 命名名称。
2. 修改驱动名称，文件过滤器 dll  so。
3. 驱动文件 加载 :  传参数  文件path 

2019-07-31

1. 打包问题：eclipse 的jar，需要用嘛？ 我要验一下？

测试沟通的问题：

2. 验一下 用户证书 英文 ' 问题 。
           测试： 支持 ' ‘’ 三种字符的用户名和主题DN		2222
3. 文本框长度限制，根据内容 : 字母、数字、中文 都遵循长度限制，   基本上都是 20   邮箱设置30   baseDN 设置100     主题(查询证书)：100    IP地址设置 15    memo 长度 为 200。
4. RADS 中申请用户证书的时候，需要自定义扩展，如果是CA确定的 但没有值的扩展需要自己添加值。
         所以CA的自定义扩展 不能自己在RADS自己添加，CLIENT的自定义扩展可以在RADS自己添加，还有一些在RADS支持的扩展可以自定义添加。 总结：模板中没有值的扩展可以在RADS添加。
5. 关于模板的修改：  
    1.  取消了 双击事件    
    2.  菜单添加了 查看、克隆、修改、作废功能(在用模板限制不能修改和作废)
 	3.  克隆模板窗口，清空模板名，光标 focus 输入框,光标退出时，显示不能为空
6.   修改管理员信息 ： 修改 RA  BA  AO  BO  
7. key 的配置文件问题，
   1. 加载或者创建   key.properties文件， 
   2. 遍历 key Name 到 combo中，
   3. 绑定 或者 解除按钮

2019-08-03 

3. 下载 子CA证书的时候，要使用 其他的模板下载，单证，服务器的resp 应该是 p7b的格式,  不是证书的格式，所以在加  格式头的时候，应该判断。
  	在下载证书选择了 csr的情况下，有这么问题，所以在  csr 的group中添加了      返回值类型 ：  radio  证书链格式   或者 是    子CA申请证书，


2019-08-12

1. 下载双证
2. SSL通信 ：  ca通信正常， kmc失败

3. CSP签名 中文问题 ： 换 jar 可以过，但是 会占用 库文件， 怎么测试 32位 和 64位的库文件  ???    jdk

5. csp 释放 key之后，异常 

6. 测试 downCert 成功之后，关闭 两个窗口





